services:
  helper_srv:
    build: .
    image: helper:latest
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - ./.env:/app/.env
    restart: always
    command: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}