services:
  helper_srv:
    build: .
    image: helper:latest
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env
    networks:
      - nginx-net
    restart: always
    command: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}

networks:
  nginx-net:
    external: true